PGM
SNDPGMMSG MSG('Ciao Dvide e Paolo!')
CRTLIB LIB(CORSOGIT)
MONMSG MSGID(CPF0000)
CRTSRCPF FILE(CORSOGIT/QCLSRC) RCDLEN(132)          
MONMSG MSGID(CPF0000)
CPYF FROMFILE(QTEMP/QCLSRC)   + 
     TOFILE(CORSOGIT/QCLSRC)  + 
     FROMMBR(*ALL)            + 
     TOMBR(*FROMMBR)          +
     MBROPT(*REPLACE)          
ENDPGM
